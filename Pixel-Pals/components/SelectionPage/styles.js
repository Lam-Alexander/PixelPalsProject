// Selection Page > styles.js
import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    titleText: {
        textAlign: 'center',
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 100,
    }
})
export default styles